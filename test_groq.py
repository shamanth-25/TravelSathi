import os
import asyncio
from backend.ai_engine import get_response

# Replace with dummy or user's key if it's in the environment. 
# Wait, we don't have the user's Groq key!
# But we can test with Ollama just to see the structure!
